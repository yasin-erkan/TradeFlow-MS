# Common Configuration for All TradeFlow Microservices
# This file contains shared database and infrastructure settings

# PostgreSQL Configuration (for services using relational database)
spring:
  datasource:
    url: jdbc:postgresql://tf_postgres:5432/tradeflow_db
    username: tradeflow_admin
    password: tf_strong_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # MongoDB Configuration (for services using NoSQL database)
  data:
    mongodb:
      # Docker internal MongoDB - use container name and internal port
      host: tf_mongodb
      port: 27017
      username: tradeflow_admin
      password: tf_strong_password
      authentication-database: admin

      # For MongoDB Atlas (Cloud) - Uncomment to use Atlas
      # uri: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

  # Kafka Configuration
  kafka:
    bootstrap-servers: tf_kafka:29092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: '*'

# Eureka Client Configuration (for all microservices)
eureka:
  client:
    service-url:
      defaultZone: http://tf-discovery-server:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

# Logging Configuration
logging:
  level:
    root: INFO
    com.tradeflow: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} - %msg%n'
